#include "Harl.hpp"

int main(void)
{
	Harl logger;
	logger.complain(std::string("fwfwe"));
	logger.complain(std::string(error));
	logger.complain(std::string(warning));
	logger.complain(std::string(info));
	logger.complain(std::string(debug));
	return (0);
}
